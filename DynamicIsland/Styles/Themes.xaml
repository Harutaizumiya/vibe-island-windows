<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="IslandBackgroundBrush" Color="#FF000000" />
    <SolidColorBrush x:Key="IslandBorderBrush" Color="#FF161616" />
    <SolidColorBrush x:Key="StatusAccentBrush" Color="#FFF5F5F5" />
    <SolidColorBrush x:Key="ActionPanelBackgroundBrush" Color="#FF000000" />
    <SolidColorBrush x:Key="ActionPanelBorderBrush" Color="#00000000" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFF5F5F5" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFB0B0B0" />
    <SolidColorBrush x:Key="TextMutedBrush" Color="#FF8A8A8A" />
    <SolidColorBrush x:Key="AccentButtonBrush" Color="#FFF2F2F2" />
    <SolidColorBrush x:Key="AccentButtonForegroundBrush" Color="#FF050505" />
    <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#FF111111" />
    <SolidColorBrush x:Key="SecondaryButtonForegroundBrush" Color="#FFF5F5F5" />
    <SolidColorBrush x:Key="TertiaryButtonBrush" Color="#FF080808" />
    <SolidColorBrush x:Key="TertiaryButtonForegroundBrush" Color="#FFD0D0D0" />

    <Style x:Key="BaseActionButtonStyle"
           TargetType="Button">
        <Setter Property="Margin" Value="4,0,4,0" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="14">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"
                                 Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="Opacity"
                                    Value="0.94" />
                        </Trigger>
                        <Trigger Property="IsPressed"
                                 Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="Opacity"
                                    Value="0.82" />
                        </Trigger>
                        <Trigger Property="IsEnabled"
                                 Value="False">
                            <Setter TargetName="ButtonBorder"
                                    Property="Opacity"
                                    Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentActionButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource BaseActionButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource AccentButtonBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentButtonForegroundBrush}" />
    </Style>

    <Style x:Key="SecondaryActionButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource BaseActionButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource SecondaryButtonBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonForegroundBrush}" />
    </Style>

    <Style x:Key="TertiaryActionButtonStyle"
           TargetType="Button"
           BasedOn="{StaticResource BaseActionButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource TertiaryButtonBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TertiaryButtonForegroundBrush}" />
    </Style>
</ResourceDictionary>
